# Where the backend API lives. The app proxies /api/* to it, so the browser only ever talks to
# this origin and the session cookie stays first-party. No trailing slash.
BACKEND_URL=http://localhost:4000
